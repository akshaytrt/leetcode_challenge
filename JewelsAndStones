class Solution:
    def numJewelsInStones(self, J: str, S: str) -> int:
        count=0
        jewels=list(J)
        stones=list(S)
        for j in jewels:
            for s in stones:
                if (j == s):
                    count=count+1
        return count
